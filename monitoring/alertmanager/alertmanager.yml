route:
  receiver: 'null'
  group_by: ['alertname', 'severity']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: 'null'

  # Uncomment when Telegram bot is ready:
  # - name: 'telegram'
  #   telegram_configs:
  #     - bot_token: '${TELEGRAM_BOT_TOKEN}'
  #       chat_id: ${TELEGRAM_CHAT_ID}
  #       parse_mode: 'HTML'
  #       message: |
  #         <b>{{ .Status | toUpper }}</b> — {{ .CommonAnnotations.summary }}
  #         {{ .CommonAnnotations.description }}
  #
  # To activate Telegram:
  # 1. Set TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID environment variables
  # 2. Change route.receiver above to 'telegram'
  # 3. Restart Alertmanager
